{{- if .Values.scVerifier.enabled }}
{{- if .Values.scVerifier.ingress.enabled }}
{{- $fullName := include "blockscout-stack.fullname" . -}}
{{- $svcPort := .Values.scVerifier.service.port -}}
{{- if and .Values.scVerifier.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ingress.annotations "kubernetes.io/ingress.class" .Values.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-sc-verifier-ingress
  labels:
    {{- include "blockscout-stack.labels" . | nindent 4 }}
  {{- with .Values.scVerifier.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.scVerifier.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.scVerifier.ingress.className }}
  {{- end }}
  {{- if .Values.scVerifier.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.scVerifier.ingress.hostname | quote }}
      secretName: {{ .Values.scVerifier.ingress.tls.secretName | default (printf "%s-sc-verifier-tls" $fullName) }}
  {{- end }}
  rules:
    - host: {{ .Values.scVerifier.ingress.hostname | quote }}
      http:
        paths:
          {{- range .Values.scVerifier.ingress.paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- else }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-sc-verifier-svc
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-sc-verifier-svc
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
{{- end }}
{{- end }}